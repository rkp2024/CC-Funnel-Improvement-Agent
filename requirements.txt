# Core dependencies for AWS deployment
pydantic==2.7.4
numpy==1.26.4
gradio>=4.0.0,<5.0.0
python-dotenv>=1.0.0

# Hugging Face Inference API for DeepSeek
# Pin to <0.23.0 to avoid HfFolder import error with Gradio 4.x
huggingface-hub>=0.20.0,<0.23.0

# Full RAG engine dependencies (sentence-transformers for optimal performance)
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
torch>=2.0.0  # Required by sentence-transformers

# Logging and analytics
pandas>=1.5.0

# API Server dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
